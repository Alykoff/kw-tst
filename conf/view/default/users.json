{
  "name":"users",
  "views":{
    "by_id": {
      "map": "function (doc, meta) { if (doc.name) { emit(doc.name, null); } } "
    },
    "by_email": {
      "map": "function (doc, meta) { if (doc.email) { emit(doc.email, null); } } "
    }
  }
}